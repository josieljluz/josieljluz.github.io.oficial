# Nome do workflow que aparecerá na interface do GitHub Actions
name: 🧹🧹Limpar Execuções de Workflow Antigas (12h)

# Configuração dos gatilhos do workflow
on:
  # Agendamento de execução automática
  schedule:
    # Cron expression que executa a cada 12 horas, no minuto 1
    # Formato: minuto hora dia-do-mês mês dia-da-semana
    # */12 - a cada 12 horas
    - cron: '1 */12 * * *'
  
  # Permite execução manual através da interface do GitHub
  workflow_dispatch:

# Definição dos jobs (tarefas) que serão executados
jobs:
  # Job principal de limpeza
  cleanup:
    # Configura o ambiente de execução (última versão do Ubuntu)
    runs-on: ubuntu-latest
    
    # Permissões necessárias para o job
    permissions:
      # Permissão para gerenciar execuções do GitHub Actions
      actions: write

    # Passos sequenciais que serão executados
    steps:
      # Passo para deletar execuções antigas
      - name: 🗑️ Deletar execuções de workflow antigas
        # Variáveis de ambiente disponíveis para este passo
        env:
          # Token de autenticação automático do GitHub
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        
        # Script shell que será executado
        run: |
          # 1. Início do processo - mensagem informativa
          echo "🔍 Buscando execuções de workflow antigas..."

          # 2. Obter todas as execuções de workflow do repositório
          # Usa a CLI do GitHub (gh) para fazer uma chamada à API
          # -H: Define o header Accept
          # /repos/{owner}/{repo}/actions/runs: Endpoint da API
          # --jq: Filtra o JSON de resposta para obter apenas id, created_at e status
          runs=$(gh api \
            -H "Accept: application/vnd.github.v3+json" \
            "/repos/${{ github.repository }}/actions/runs" \
            --jq '.workflow_runs[] | {id, created_at, status}')

          # 3. Verifica se há execuções para processar
          if [ -z "$runs" ]; then
            echo "✅ Nenhuma execução de workflow encontrada."
            # Sai com código 0 (sucesso) se não houver execuções
            exit 0
          fi

          # 4. Obtém o timestamp atual em segundos (UTC)
          now=$(date -u +%s)

          # 5. Processa cada execução encontrada
          # Usa jq para iterar sobre cada objeto JSON
          # -c: Saída compacta (uma linha por objeto)
          echo "$runs" | jq -c 'select(.created_at != null)' | while read -r run; do
            # 5.1 Extrai o ID da execução
            id=$(echo "$run" | jq -r '.id')
            
            # 5.2 Extrai a data de criação
            created_at=$(echo "$run" | jq -r '.created_at')

            # 5.3 Converte a data de criação para timestamp Unix
            # Tenta primeiro com formato GNU (Linux)
            if date -u -d "$created_at" >/dev/null 2>&1; then
              run_date=$(date -u -d "$created_at" +%s)
            # Se falhar, tenta formato BSD (macOS)
            elif date -u -j -f "%Y-%m-%dT%H:%M:%SZ" "$created_at" >/dev/null 2>&1; then
              run_date=$(date -u -j -f "%Y-%m-%dT%H:%M:%SZ" "$created_at" +%s)
            # Se ambos falharem, registra erro e continua
            else
              echo "⚠️ Não foi possível analisar a data: $created_at"
              continue
            fi

            # 5.4 Calcula a diferença de tempo em segundos
            diff_seconds=$(( now - run_date ))
            # Converte para horas (3600 segundos = 1 hora)
            diff_hours=$(( diff_seconds / 3600 ))

            # 5.5 Verifica se a execução tem mais de 12 horas
            if [ "$diff_hours" -ge 12 ]; then
              # 5.5.1 Log informativo antes de deletar
              echo "🗑️ Deletando execução de workflow ID: $id (Criada há $diff_hours horas)"
              
              # 5.5.2 Chamada à API para deletar a execução
              gh api \
                --method DELETE \
                -H "Accept: application/vnd.github.v3+json" \
                "/repos/${{ github.repository }}/actions/runs/$id" \
                >/dev/null 2>&1 || echo "⚠️ Falha ao deletar execução ID: $id"
            else
              # 5.6 Log para execuções que serão mantidas
              echo "✅ Execução de workflow ID: $id ainda não tem 12 horas (Criada há $diff_hours horas)"
            fi
          done
          
          # 6. Mensagem final indicando conclusão do processo
          echo "🏁 Processo de limpeza concluído!"
